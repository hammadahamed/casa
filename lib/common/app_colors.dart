import 'package:flutter/material.dart';

class AppColors {
  static const bg = Color(0xFF151515);
  static const bgDark = Color(0xFF0f0f0f);
  static const bgContainer = Color.fromARGB(255, 27, 27, 27);
}
